using Test
using TMCore

@testset "TMCore" begin
    # Placeholder: the package loads. Real tests arrive with the evaluator, once the clause-vote
    # ceiling is settled — see the README's open questions.
    @test isdefined(Main, :TMCore)
end
